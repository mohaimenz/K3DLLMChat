apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: k3d-llm-sidecar
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
