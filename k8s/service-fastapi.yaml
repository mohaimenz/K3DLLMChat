apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
  namespace: k3d-llm-sidecar
spec:
  type: ClusterIP
  selector:
    app: inference-api
  ports:
    - name: api
      port: 8000
      targetPort: 8000
      protocol: TCP
